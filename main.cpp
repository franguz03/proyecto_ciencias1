#include <cstdlib>
#include <iostream>
#include "Ciudad.h"
#include "Persona.h"
#include <iomanip>
#include <list>

using namespace std;

int main(int argc, char *argv[]){

	/*
	struct tm fechaNacimiento = {0, 0, 0, 1, 0, 100};
	Persona p("Juan", "Perez", 123456789, 'M', "Soltero", "Medellin", "Bogota", fechaNacimiento);
	list<Persona> listaDePersonas;
	listaDePersonas.push_back(p);
	cout << "Initial size of list = " << listaDePersonas.size() << endl;
	Persona x= listaDePersonas.front();//devuelve el primer elemento
	cout<<x.apellido;
	*/	
}

